import { DataLoader, LoaderSpinner } from "./styled/widgets/Widgets";

const DataLoading = () => {
  return (
    <DataLoader>
      <LoaderSpinner />
    </DataLoader>
  );
};

export default DataLoading;
